This analyzer depends on pefile, hashlib, magic, pydeep, pyexifinfo, pehashng

pehashng is installed in lib/
The other dependencies can be installed with pip :

```
pip install pefile hashlib magic pydeep pyexifinfo
```
